import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '../ui/card';

export function WidgetSkeleton() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>WidgetSkeleton</CardTitle>
      </CardHeader>
      <CardContent>
        Placeholder for WidgetSkeleton
      </CardContent>
    </Card>
  );
}
